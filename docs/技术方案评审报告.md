# 技术方案评审报告（最新复评）

评审日期：2026-03-25  
评审对象：AI 电商项目（`frontend/` + `backend/`）  
评审类型：整改后再次复评（基于当前仓库代码与最新测试产物）

## 1. 执行摘要

总体结论：**阶段性通过（已进入 Phase 2）**。  
上一轮高优先级问题已全部关闭，后端稳定性与可回归能力达到目标；当前主要短板集中在前端覆盖率不足与部分后端路由覆盖率结构不均衡。

本次关键指标（以本地最新执行结果为准）：
- 后端测试：`65 passed`
- 后端覆盖率：`51%`（已达阶段目标 `>=50%`）
- 前端测试：`22 passed`
- 前端覆盖率：`7.71%`（较 `1.92%` 提升，但未达 `>=20%` 目标）

## 2. 问题闭环状态

### 2.1 已关闭问题

1. 订单支付状态不一致：**已关闭**
- `POST /api/orders/{order_id}/pay` 兼容 `pending -> paid`，且对 `paid` 幂等返回。

2. AI 向量同步权限偏宽：**已关闭**
- `POST /api/ai/products/{product_id}/sync-embedding` 已收敛为管理员权限。

3. Cookie `secure` 硬编码：**已关闭**
- 已引入 `COOKIE_SECURE` 配置，按环境控制。

4. 管理员订单状态迁移松散：**已关闭**
- 已接入显式状态迁移矩阵，阻断非法跳转。

5. Pydantic v2 `class Config` 告警：**已关闭**
- Schema 已统一迁移为 `ConfigDict`，后端测试运行不再出现该类警告。

6. 后端覆盖率不足：**已关闭（当前阶段）**
- 从 `28.33%` 提升至 `51%`。

### 2.2 部分完成问题

1. 前端测试覆盖率不足：**进行中**
- 从 `1.92%` 提升到 `7.71%`，但距离 `>=20%` 仍有缺口。缺少页面交互测试

## 3. 当前风险清单

### 高优先级

当前未发现新的高优先级阻断问题。

### 中优先级

1. 前端覆盖率仍偏低（`7.71%`）
- 风险：页面级回归（登录页、订单页、AI 助手 UI）仍可能在重构时漏检。

2. 后端覆盖率结构不均衡
- 总覆盖率达标，但路由模块仍有明显差异：
  - `_product.py`：`40%`
  - `_order.py`：`28%`
  - `_auth.py`：`38%`
- 风险：复杂分支仍需更细颗粒回归保护。

### 低优先级

1. `ai_service.py` 仍为大文件（`840` statements），建议按职责拆分。
2. 可补充契约测试（OpenAPI schema 快照）与错误码快照测试。

## 4. 评分更新

| 维度 | 上轮复评 | 本次复评 | 变化 |
|---|---:|---:|---|
| 架构与技术选型 | 23/25 | 23/25 | 持平 |
| 业务模型落地 | 24/25 | 24/25 | 持平 |
| 安全与鉴权 | 22/25 | 23/25 | 提升（配置化与权限收敛） |
| 可维护性与测试 | 15/25 | 18/25 | 提升（后端覆盖率达标） |
| **总分** | **84/100** | **88/100** | **+4** |

## 5. 下一阶段建议（2026-03-25 至 2026-04-08）

1. 前端覆盖率冲刺到 `>=20%`
- 优先页面：`Login`、`Home`、`Orders`、`ClientAiAssistant`。
- 优先链路：登录刷新联锁、SSE 事件渲染、角色守卫跳转。

2. 后端路由补盲
- 把 `_order.py` 和 `_product.py` 分支覆盖拉升到 `>=55%`。
- 增加异常路径测试：库存不足、分类层级非法、上传异常 MIME/尺寸。

3. AI 服务可维护性治理
- 拆分 `ai_service.py` 为意图识别、召回检索、卡片编排三个模块。
- 先做“无行为变化”的重构，再补单元测试。

## 6. 最终结论

项目已完成从“可用”到“可回归”的关键跃迁：高风险项闭环、后端覆盖率达标、安全策略可配置。  
当前建议继续推进前端测试覆盖与 AI 服务模块化，以支撑下一阶段迭代稳定性。

