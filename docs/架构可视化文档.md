# 架构可视化文档

更新时间：2026-03-25

## 1. 架构目标

- 单体轻量：单后端服务 + 单 SQLite，拒绝过度拆分。
- 前后端清晰分层：React 端负责交互，FastAPI 负责鉴权与业务编排。
- AI 路由与业务计算分离：模型只做表达，业务数据由后端先计算后注入。

## 2. 系统上下文图

```mermaid
flowchart TB
    subgraph Browser["浏览器"]
        FE["React 19 + Vite + AntD + Zustand"]
    end

    subgraph Server["FastAPI 单体服务"]
        API["API Routers\n/auth /products /orders /addresses /ai /admin"]
        SVC1["order_service\n事务/库存/快照"]
        SVC2["ai_service\n意图识别/向量检索/SSE"]
        AUTH["JWT + deps\nget_current_user/admin"]
    end

    subgraph Data["数据层"]
        DB["SQLite (WAL)\ncheck_same_thread=False"]
        UP["uploads/ 静态资源"]
    end

    subgraph Model["模型服务"]
        LLM["OpenAI SDK\nChat + Embeddings"]
    end

    FE -->|HTTP /api| API
    FE -->|静态资源 /uploads| UP
    API --> AUTH
    API --> SVC1
    API --> SVC2
    AUTH --> DB
    SVC1 --> DB
    SVC2 --> DB
    SVC2 --> LLM
```

## 3. 前后端分层图

```mermaid
flowchart LR
    subgraph Frontend
        R["router/index.tsx\ncreateBrowserRouter + PrivateRoute"]
        ST["Zustand\nuseAuthStore/useCartStore"]
        NET["request.ts\nAxios + Refresh + SSE"]
        UI["Ant Design + Ant Design X + styled-components"]
    end

    subgraph Backend
        RT["Routers"]
        DP["deps.py\n鉴权与权限"]
        SC["schemas/*.py\nPydantic v2"]
        SV["services/*.py\n业务事务与AI编排"]
        ORM["models.py\nSQLAlchemy 2.0 Async"]
    end

    R --> NET
    ST --> NET
    NET --> RT
    RT --> DP
    RT --> SC
    RT --> SV
    SV --> ORM
```

## 4. 数据模型 ER 图（核心表）

```mermaid
erDiagram
    USERS {
        int id PK
        string username
        string email
        string role
        int token_version
        int is_active
    }

    REFRESH_TOKENS {
        int id PK
        int user_id FK
        string token_hash
        string expires_at
    }

    ACCESS_TOKEN_BLOCKLIST {
        int id PK
        int user_id FK
        string jti
        string expires_at
    }

    USER_ADDRESSES {
        int id PK
        int user_id FK
        string receiver_name
        string phone
        int is_default
    }

    PRODUCT_CATEGORIES {
        int id PK
        string category_code
        int parent_id
        int level
    }

    PRODUCTS {
        int id PK
        string name
        float price
        int stock
        int category_id FK
        string status
    }

    PRODUCT_EMBEDDINGS {
        int product_id PK,FK
        string content_hash
        text embedding
    }

    PRODUCT_EMBEDDING_STATUSES {
        int product_id PK,FK
        string status
        string last_error
    }

    ORDERS {
        int id PK
        string order_no
        int user_id FK
        float total_amount
        string status
        text receiver_info
    }

    ORDER_ITEMS {
        int id PK
        int order_id FK
        int product_id FK
        string product_name
        float buy_price
        int quantity
    }

    CHAT_MESSAGES {
        int id PK
        int user_id FK
        string role
        text content
        text payload
    }

    USERS ||--o{ USER_ADDRESSES : has
    USERS ||--o{ ORDERS : places
    USERS ||--o{ CHAT_MESSAGES : owns
    USERS ||--o{ REFRESH_TOKENS : has
    USERS ||--o{ ACCESS_TOKEN_BLOCKLIST : has

    PRODUCT_CATEGORIES ||--o{ PRODUCTS : classifies
    PRODUCTS ||--|| PRODUCT_EMBEDDINGS : vector
    PRODUCTS ||--|| PRODUCT_EMBEDDING_STATUSES : vector_status

    ORDERS ||--|{ ORDER_ITEMS : contains
    PRODUCTS ||--o{ ORDER_ITEMS : referenced
```

## 5. 关键业务时序图

### 5.1 下单事务（防超卖 + 快照固化）

```mermaid
sequenceDiagram
    participant FE as Checkout
    participant OR as /api/orders
    participant SVC as create_order_transaction
    participant DB as SQLite

    FE->>OR: POST /api/orders
    OR->>SVC: user_id + address_id + items
    SVC->>DB: BEGIN IMMEDIATE
    loop each item
        SVC->>DB: UPDATE products SET stock=stock-qty WHERE id=? AND stock>=qty
        DB-->>SVC: rowcount
    end
    SVC->>DB: SELECT address (校验归属)
    SVC->>DB: INSERT orders(receiver_info JSON)
    SVC->>DB: INSERT order_items(buy_price 快照)
    SVC->>DB: COMMIT
    OR-->>FE: OrderResponse
```

### 5.2 鉴权联锁清退（401/403）

```mermaid
sequenceDiagram
    participant FE as Axios Interceptor
    participant API as FastAPI deps
    participant DB as SQLite

    FE->>API: Bearer Access Token
    API->>DB: 校验 jti 是否拉黑 + token_version + is_active
    alt Token 过期或失效
        API-->>FE: 401
        FE->>API: POST /auth/refresh (Cookie: refresh_token)
        API->>DB: 校验 refresh_tokens
        alt 刷新成功
            API-->>FE: 新 access_token
            FE->>API: 重放原请求
        else 刷新失败
            API-->>FE: 401
            FE->>FE: forceLogout()
        end
    else 用户被封禁/越权
        API-->>FE: 403
        FE->>FE: forceLogout() 并跳转登录页
    end
```

## 6. 部署视图（本地/单机）

```mermaid
flowchart LR
    U[User Browser] --> N[Nginx/Vite Dev Server]
    N -->|/api| B[FastAPI :8000]
    N -->|/uploads| B
    B --> D[(SQLite file)]
    B --> M[LLM API]
```

## 7. 架构结论

- 已满足“轻量化单体 + SQLite + 事务一致性 + SSE AI 接口”的核心要求。
- 订单快照、防踢出、安全鉴权均已落地在真实代码路径中。
- 当前最需要加强的是测试覆盖与 AI 服务模块复杂度治理（见《技术方案评审报告》）。

